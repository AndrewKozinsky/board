import {store} from '../store/store.ts'

export const canvasMove = {
	/*setEventListeners() {
		store.app.canvas.addEventListener('wheel', (event) => {
			const {deltaX, deltaY} = event
			this.moveCanvas(deltaX, deltaY);
		}, {passive: true})
	},*/

	/*moveCanvas(relativeX: number, relativeY: number) {
		store.app.stage.position.x -= relativeX
		store.app.stage.position.y -= relativeY
	}*/
}
