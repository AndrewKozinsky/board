import {Graphics, Text} from 'pixi.js'
import {store} from '../store/store.ts'

export const renderCanvas = {
	/*drawScene() {
		const circle = new Graphics()
			.circle(250, 250, 200)
			.fill(0xFF0000)

		// Add the circle to the stage
		store.app.stage.addChild(circle)

		// Position the circle in the middle of the canvas
		circle.x = (store.app.canvas.width / (store.canvas.devicePixelRatio * 2)) - (circle.width / 2)
		circle.y = (store.app.canvas.height / (store.canvas.devicePixelRatio * 2)) - (circle.height / 2)

		const text = new Text({text: 'some text', style: {fontSize: 70}})

		store.app.stage.addChild(text)
	}*/
}
