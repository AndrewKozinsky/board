import {Container, ContainerChild, Text} from 'pixi.js'

export const pixiHelper = {
	/*findAllTextObjects(container: Container<ContainerChild>, result = []) {
		container.children.forEach(child => {
			if (child instanceof Text) {
				result.push(child);
			}
			// If the child is a container, search its children
			if (child instanceof Container) {
				this.findAllTextObjects(child, result);
			}
		})

		return result
	}*/
}
